# Frontend environment variables (build-time)
#
# In production (Docker + Nginx), the frontend uses relative /api/ URLs
# that Nginx proxies to the backend. No VITE_API_BASE_URL is needed.
#
# For local development outside Docker (npm run dev), create .env.local:
#   VITE_API_BASE_URL=http://localhost:8080
